---
# Generated by Tycho {{ now() }}
apiVersion: v1
kind: Service
metadata:
  labels:
    name: {{ system.name }}
  name: {{ system.name }}
resourceversion: v1
spec:
#  type : NodePort
  type: LoadBalancer
  selector:
    name: {{ system.name }}
  ports:
  - name: port
    port: {{ container_port }}
    protocol: TCP
    targetPort: {{ container_port }}
    
