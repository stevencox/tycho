---
# Generated by Tycho {{ now() }}
apiVersion: v1
kind: Service
metadata:
  labels:
    name: {{ service.name }} 
    executor: tycho
    tycho-guid: {{ system.identifier }}
  name: {{ service.name }} 
resourceversion: v1
spec:
  type: LoadBalancer
  selector:
    name: {{ system.name }}
  ports:
  - name: port
    port: {{ service.port }}
    protocol: TCP
    targetPort: {{ service.port }}
    
